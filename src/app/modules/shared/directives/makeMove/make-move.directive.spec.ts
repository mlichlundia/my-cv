import { MakeMoveDirective } from './make-move.directive';

describe('MakeMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeMoveDirective();
    expect(directive).toBeTruthy();
  });
});
