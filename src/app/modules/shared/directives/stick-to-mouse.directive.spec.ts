import { StickToMouseDirective } from './stick-to-mouse.directive';

describe('StickToMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new StickToMouseDirective();
    expect(directive).toBeTruthy();
  });
});
